# F.O.R.G.E. does not require environment variables for core usage.
# Keep this file as the safe template for future local-only settings.
#
# Do not commit secrets or machine-specific paths in `.env`.
